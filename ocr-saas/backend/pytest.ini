[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Pure unit tests (no I/O)
    integration: Tests requiring database/Redis/RabbitMQ
    load: Load tests (use locust, not pytest)
